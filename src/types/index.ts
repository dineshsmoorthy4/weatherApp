export interface WeatherData {
    cityName: string;
    temperature: number;
    condition: string;
    description: string;
    icon: string;
  }
  
  export interface ThemeColors {
    backgroundColor: string;
    textColor: string;
    cardBgColor: string;
    inputBgColor: string;
  }